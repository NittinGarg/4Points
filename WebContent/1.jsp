<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
    pageEncoding="ISO-8859-1"%>
<!DOCTYPE html>
<%@ page import="java.*" %>
<%@ page import="java.sql.*"%>

<%@ page import="asd.DbConnection1" %>

<%

	
		/* String category = request.getParameter("pcat");
		String name = request.getParameter("pname");
		String price = request.getParameter("pprice");
		String quantity = request.getParameter("pquantity");
		String user = "maggo";
		//String name1 = "maggo";int price1 = 1;int quantity11 = 1;
		int price1 = Integer.parseInt(price);
		int quantity1 = Integer.parseInt(quantity);
		  Connection  con = DbConnection1.dbConnection("mssql");
		  PreparedStatement stmt  =null;
			
		  stmt =con.prepareStatement("insert into addtocart (username, productname, productprice, productquantity, productcat)values (?,?,?,?,?)");
			
			stmt.setString(1, user);
			stmt.setString(2, name);
			stmt.setInt(3, price1);
			stmt.setInt(4, quantity1);

			stmt.setString(5, category);
			stmt.executeUpdate();
		   */
		  
		  
		  
		//UsersTable.cart(user,name,price1,quantity1,category, con);
		//	request.setAttribute("category1", category);
			
		//	request.getrequestuestDispatcher("Addproduct1.jsp").forward(request, res);
			//res.sendRedirect("Addproduct1.jsp");
			
	
%>
